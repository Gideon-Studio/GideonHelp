mode: MENU # Options: TEXT | MENU | COMMAND
execute-command: "menuhelp" # Only used if mode = COMMAND

prefix: "&7[&bGideonHelp&7] "

messages:
  no-permission: "&cYou don't have permission to use this command."
  current-mode: "&7Current mode: &a%mode%"
  mode-changed: "&aMode changed to &b%mode%"
  tab-blocked: "&7Unknown command."

# ─────────────────────────────
# 💬 Text Mode (shown when mode: TEXT)
# ─────────────────────────────
text-help:
  - "&b/help &7- Shows this help menu"
  - "&a/spawn &7- Teleport to spawn"
  - "&a/tpa <player> &7- Send a teleport request"
  - "&a/home &7- Go back to your home"


# ─────────────────────────────
# 🧱 Tab Complete Blocker
# ─────────────────────────────
tab-complete-blocker:
  enabled: true           # true = block tab completions
  admin-bypass: true      # true = admins with 'gideonhelp.admin' can see all commands
  allowed-commands:
    - help
    - spawn
    - home
  message: "&7Unknown command."

# ─────────────────────────────
# 🔒 Command Blocker
# ─────────────────────────────
command-blocker:
  enabled: true           # true = block these commands
  admin-bypass: true      # true = admins with 'gideonhelp.admin' bypass the block
  blocked-commands:
    - pl
    - plugins
    - ver
    - version
  message: "&cYou don't have permission to use this command."
